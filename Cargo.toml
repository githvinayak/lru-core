[package]
name = "lru-core"
version = "0.1.0"
edition = "2024"

[dependencies]
 thiserror = "2"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "cache_bench"
harness = false