[package]
name = "lru-core"
version = "0.1.0"
edition = "2024"

[dependencies]
 thiserror = "2"
tokio = { version = "1", features = ["full"] }
async-trait = "0.1"
tokio-stream = "0.1"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "cache_bench"
harness = false